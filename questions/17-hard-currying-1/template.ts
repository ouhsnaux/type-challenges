// type Rest<T> = T extends [first: ]
declare function Currying<F extends Array<any>, R>(fn: any) {
  if (rest.length === 0) {

  }
}

const r = Currying((a: string, b: number, c: boolean) => true)
type t = typeof r
